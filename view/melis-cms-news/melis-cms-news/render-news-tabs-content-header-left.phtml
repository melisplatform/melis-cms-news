<?php 
    /**
     * Disabling workflow functionality when Melis Small Business is disabled
     */
    $zoneConfigInterface = $this->zoneconfig['interface'];
    //do not show the workflow button if news id is not given
    if (empty($this->newsId)) {
        unset($zoneConfigInterface['meliscmsnews_content_tabs_properties_header_workflow']);
    }
	if(!empty($zoneConfigInterface)) {
	    foreach($zoneConfigInterface as $keyConfig => $menuConfig) {
	        echo $this->$keyConfig;
	    }
	}
?>